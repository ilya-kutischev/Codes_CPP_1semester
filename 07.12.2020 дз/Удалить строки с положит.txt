/*Составить функцию, которая для одномерного массива определяет, содержит он положительные элементы или нет.

Используя функцию, в матрице matr(n, m) удалить строки, не содержащие положительных элементов. Дополнительный массив не использовать. */

#include <iostream>

using namespace std;

const int D1 = 20, D2 = 20;
int ifPolozh(int a[], int n);

int main() {

	int a[D1][D2];

	int i, j, n, m;
	cout << "N="; //ввод количества строк
	cin >> n;
	cout << "M="; //ввод количества столбцов
	cin >> m;
	cout << "Input matrix A \n";
	//цикл по переменной i, в которой перебираем строки матрицы
	for (i = 0; i < n; i++)
		//цикл по переменной j, в котором перебираем элементы внутри строки
		for (j = 0; j < m; j++)
			cin >> a[i][j]; //ввод очередного элемента матрицы


	int x[D1];
	for (int i = 0; i < n; i++)

		if (ifPolozh(a[i], m)) {                   //проверка на положительные
			for (int j = i; j < n; ++j) {
				for(int k=0;k<m;k++)
				a[j][k] = a[j + 1][k];   //удаление строки
			}
			--n;
		}






	cout << "matrix A \n";
	for (i = 0; i < n; i++)
	{
		//цикл по переменной i, в котором перебираем строки матрицы
		for (j = 0; j < m; j++)
			cout << a[i][j] << "\t"; //вывод очередного элемента матрицы
		cout << endl; //переход на новую строку после вывода всех элементов строки
	}
	system("pause");
	return 0;
}

int ifPolozh(int a[], int n) {
	int fl = 0;

	for (int i = 0; i < n; i++) {

		if (a[i] > 0) { fl = 1; break; }   //сравнение


	}


	return fl;
}
